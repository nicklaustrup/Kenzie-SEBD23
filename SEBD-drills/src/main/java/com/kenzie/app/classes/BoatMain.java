package com.kenzie.app.classes;

public class BoatMain {

    // Create a class that holds a list of passengers
    // The boat can hold up to 300 passengers
    public static void addPassenger() {

    }

    // Launch the boat
    // This changes whether the boat is "at port" or "not at port"
    public static void launch() {

    }

    // Sink the boat
    // This changes the boat from "is floating" or "is not floating"
    public static void sink() {

    }

    public static void main(String[] args) {
        addPassenger();
        launch();
        sink();
    }
}
